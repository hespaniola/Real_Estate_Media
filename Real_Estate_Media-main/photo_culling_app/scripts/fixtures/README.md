# Test fixtures

`sample.heic` is the `libheif` project's own example image
(`examples/example.heic` from https://github.com/strukturag/libheif),
used here only to verify the HEIC decode path in
`scripts/test-integration.mjs` against a real HEIF file rather than a
synthetic one.
